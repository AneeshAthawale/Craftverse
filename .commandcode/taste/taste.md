- Prefers removing dead/unused code — including supporting state/logic for features already visually removed or commented out (e.g. cleaning up "removed as requested" code). Confidence: 0.9
- For any feature spanning multiple clients, prefers ONE server-authoritative real-time channel (the existing Socket.IO layer) — explicitly rejects BroadcastChannel, localStorage game state, or frontend-authoritative state as coordination/sync mechanisms. Confidence: 0.85
- Prefers restrained, minimalist visual design: dark/near-black surfaces, thin borders, a single subtle accent color, muted secondary text, compact spacing; avoids gradients, heavy animation, giant illustrations/logos, emojis, and decorative clutter (e.g. "subtle theme × feature", not a fan-site replica). Confidence: 0.85
- Prefers non-blocking, in-context UI feedback for transient status (e.g. a small corner countdown chip) over modals, full-screen overlays, or blocking screens; reserves full-area panels for terminal states (e.g. disqualified/completed). Confidence: 0.75
- Prefers adding clean placeholder content to stub/incomplete files (pages, layout, reusable UI components) so they are ready to be built on, rather than leaving them empty or mislabeled. Confidence: 0.8
- Prefers new code to match existing codebase conventions (React functional components with named exports, className hooks into existing styles). Confidence: 0.7
- Verifies the build compiles cleanly after making changes before reporting done. Confidence: 0.7
- When doing a visual redesign/restyle, keeps existing mock/hardcoded data (IDs, statuses, sample values) as-is — restyles and relabels only rather than enriching or changing the data. Confidence: 0.8
- Prefers avoiding new dependencies/external assets for styling — chooses existing system fonts and CSS-only decoration over adding webfont imports or libraries. Confidence: 0.8
- Effort/ecosystem note: this machine's shell has `NODE_ENV=production` set, causing `npm install` to skip devDependencies (vite/eslint). Clear it (`set NODE_ENV= &&`) when installing/fixing dev tooling. Confidence: 0.8
- Prioritizes functionality and correct data flow over visual polish — defers animations, decorative backgrounds, gradients, and redesign work until the app is functional. Confidence: 0.97
- Prefers not rewriting existing working code; extends or keeps it unless the change is required. Confidence: 0.9
- Prefers phased development: complete one phase, stop, report status (what's functional, what remains mocked, next steps), and do not auto-proceed to the next phase. Confidence: 0.9
- Report depth follows the user's request: sometimes asks for brief summaries, sometimes for a detailed walkthrough (e.g. "tell me in detail what you did", "tell me what the real problem was and how you solved it") covering exploration, root cause vs symptom, changes, verification, and debugging — give the level of detail explicitly requested. Confidence: 0.9
- Expects end-to-end runtime verification before reporting done — start the services, test the core flows, confirm existing pages still load, and fix any introduced errors. Confidence: 0.85
- Prefers keeping mock data until real backend endpoints exist; explicitly dislikes faking successful backend operations. Confidence: 0.9
- Wants secrets/config in environment variables, never hardcoded credentials. Confidence: 0.95
- Frontend must never connect to the database directly — all data flows React → API/Socket.IO → Express → PostgreSQL. Confidence: 0.95
- Prefers a single shared API service and single Socket.IO helper in the frontend — no competing API clients or duplicate socket connections. Confidence: 0.95
- Business logic stays in the backend; does not want it duplicated in React components. Confidence: 0.9
- Wants loading, error, empty, and socket-disconnected states handled gracefully in the UI. Confidence: 0.85
- Requires real-time event subscriptions (socket listeners) to be cleaned up on unmount and only the events relevant to the feature to be subscribed at all. Confidence: 0.65
- Wants phase-completion reports structured with specific sections: real vs mock data, endpoints used, socket events, DB changes, tests/results, remaining work. Confidence: 0.8remaining work. Confidence: 0.8s mocked, test results, backend gaps discovered, and recommended next phase. Confidence: 0.85
- Corrects changes that overreach the requested scope — apply fixes narrowly to exactly what was asked, and do not silently alter adjacent code/credentials (e.g., a participant/team password fix must not also change dev/admin passwords or flatten roles to one shared value). Confidence: 0.85articipant/team password fix must not also change dev/admin passwords or flatten roles to one shared value). Confidence: 0.85
- When a reported behavior is missing, wants a root-cause trace of the full event flow (UI click → socket emit → backend validate → broadcast → client state → render) with browser console and backend logs to locate exactly where the state is lost — explicitly prefers diagnosing over immediately rewriting/rebuilding the system. Confidence: 0.8
- Verifies real-time cross-role UI features on the actual end-user's rendered screen using two isolated real browsers (e.g., an admin and a team player), not just via API/socket-level checks or the admin/control view alone — including player refresh, late-join, and disconnect/reconnect occurring mid-flow. Confidence: 0.8
- For state-machine/game behavior, explicitly verifies the boundary semantics that matter (typing during an in-flight transition/countdown vs. after the flip, before vs. after a deadline), driven from the actual UI, and keeps the authoritative server state as ground truth when timing races make the browser flaky. Confidence: 0.7
- Prefers tab-scoped auth sessions: stores the JWT in sessionStorage (not localStorage) so each browser tab keeps an independent identity, and keeps ONE frontend token source used consistently by login, session restore (/auth/me), API helpers, socket auth, role checks, and logout — never two competing token stores. Confidence: 0.9
- When re-authenticating (login/logout or identity change), tears down the existing Socket.IO connection before reconnecting with the new token, and captures the token per connection at connect time — never a module-global token that another tab can overwrite, never resurrecting a stale/old-token socket. Confidence: 0.85
- For rule-enforced game states (e.g. an illegal-action phase), prefers keeping the UI interactive and detecting/reporting the violation to the authoritative backend for adjudication over disabling or blocking the input client-side to prevent it. Confidence: 0.8
- Rule-enforcement/monitoring code must never swallow legitimate user input: a violation-detection keydown listener must report activity without calling preventDefault (or otherwise blocking), so keystrokes still insert text normally in the editor in every state (GREEN, countdown, RED) while detection continues to fire. Confidence: 0.85
- When the user reverts a whole batch of changes because the output was undesired, treats the prior prompt and its context as void — does not continue, salvage, or reference the discarded work, and waits for the next fresh prompt. Confidence: 0.6
- Expects an inspect-first workflow for feature work: thoroughly map the existing schema/migrations, auth & RBAC, routes/controllers/services, Socket.IO events, frontend pages, and tests before designing or writing code — then implement the smallest coherent change set for the phase (prompts often say "do NOT start writing code — inspect first", then enumerate what to inspect). Confidence: 0.9
- When removing an obsolete role/access path, expects a whole-codebase sweep (DB enums/check constraints, route guards, controllers, Socket.IO gates, frontend role checks, seeds, and tests) with a purpose-based decision per usage — never a blind find-replace of the removed role. Confidence: 0.8
- When lint/tests/build surface errors, wants them fixed at the root cause rather than suppressed or silenced (e.g. delete an unused import instead of adding eslint-disable comments; fix rather than swallow failing cases). Confidence: 0.75
- When asking for a codebase survey to estimate remaining scope, expects a strictly read-only analysis ("Don't do any changes") delivered in brief as a layered inventory — what already exists (DB, backend, frontend, tests) vs. what remains to build — so they can size upcoming work; no edits or implementation during such an overview request. Confidence: 0.7